'use client'
import { cn } from '@/lib/utils';
import hotToaster, { Toaster as HotToaster, Toaster } from 'react-hot-toast';

const toast = () => {
    return (
        <div>
        </div>
    )
};

export default toast;
